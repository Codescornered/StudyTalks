import { useState } from "react";
export default function FlashCardForm({ onPost }) {
    const [flip, setFlip] = useState(false);

    <div>
        <div className="front ">
            {flashcard.question}
            <div>

            </div>
        </div>
    </div>
}
